# CLAUDE.md — Notary CDD & Risk Assessment WebApp

## Ringkasan Proyek
Aplikasi web lokal untuk kantor notaris di Indonesia. Digunakan untuk:
1. Mengisi Formulir Customer Due Diligence (CDD) — 3 jenis: Korporasi, Perorangan, dan Perikatan Lainnya (Legal Arrangement)
2. Melakukan Penilaian Tingkat Risiko pengguna jasa secara otomatis (skoring berbasis tabel referensi)
3. Mengisi Informasi Tambahan untuk pengguna jasa berisiko Tinggi (Enhanced Due Diligence)

Dasar hukum: PP No. 43 Tahun 2015, Permenkumham No. 9 Tahun 2017 (Formulir CDD), Perpres No. 13 Tahun 2018, dan Permenkumham No. 15 Tahun 2019 (Pemilik Manfaat/Beneficial Owner).

## Tech Stack
- **Frontend + Backend:** Next.js 14+ (App Router, TypeScript, Tailwind CSS) — satu project, satu proses
- **Database:** SQLite (file lokal), diakses via Prisma ORM
- **Mutasi data:** Server Actions Next.js (bukan REST API terpisah)
- **Deployment:** Lokal saja — `npm run dev`, satu kantor notaris, tanpa hosting/cloud

## Sumber Kebenaran (Source of Truth)
Semua field form, label, dan tabel skor risiko WAJIB mengacu ke `reference-data.md`. File itu adalah satu-satunya sumber kebenaran untuk domain data. Jangan menambah, mengubah, atau menghilangkan field tanpa terlebih dahulu mengupdate file tersebut.

## Known Gaps (per inisialisasi proyek)
1. **Tabel Referensi "Profil Bisnis" (RefBusinessSectorScore)** — kategori dan skor resmi belum tersedia dari dokumen sumber. Tabel dibuat kosong. Total Nilai risiko TIDAK dianggap final sampai tabel ini terisi.
2. **Form EDD Korporasi/Institusi** — dokumen sumber (halaman 15/29) hanya menyertakan section Perorangan. Form EDD untuk Korporasi/Legal Arrangement belum ada.

Detail lengkap ada di `reference-data.md` bagian 9 (Known Gaps). Ketika data ini tersedia, gunakan PROMPT 3 di `claude-code-prompt.md`.

## Asumsi v1
- Single user / satu notaris, tanpa login-autentikasi (bisa ditambahkan sebagai update terpisah)
- Data tersimpan lokal (file SQLite), tidak ada sinkronisasi cloud
- Export PDF/print disertakan sebagai fitur dasar untuk kebutuhan arsip fisik

## Riwayat Perubahan
| Versi | Tanggal | Perubahan | File Terdampak | Keterangan |
|-------|---------|-----------|-----------------|------------|
| v1 | 2026-08-01 | Initial build | Semua file | Scaffold project, schema, seed data, 3 form CDD, modul risk assessment, form EDD, halaman admin referensi, dashboard, export PDF. Validasi akhir: 121 field dari reference-data.md, 121 ditemukan di schema+UI. Gap tersisa: RefBusinessSectorScore (Tabel B) kosong, form EDD Korporasi/Institusi belum tersedia — keduanya Known Gap terdokumentasi, bukan kesalahan implementasi. |
| v2-fase1 | 2026-08-04 | Phase 2 PRD — Fase 1: akses & safety net | `package.json`, `proxy.ts` (baru), `lib/auth.ts` (baru), `lib/actions/auth.ts` (baru), `app/lock/`, `components/auth/PinForm.tsx` (baru), `.env.example` (baru), `scripts/hash-pin.ts` (baru), `SETUP.md` (baru), `lib/storage.ts`, `lib/actions/backup.ts` (baru), `app/api/backup/[filename]/route.ts` (baru), `app/admin/backup/`, `components/admin/BackupPanel.tsx` (baru), `app/layout.tsx`, `.gitignore` | FR-6A (bind 127.0.0.1), FR-6B (PIN gate via Proxy — nama baru untuk middleware di Next.js 16 — dengan lockout 3x/5 menit, sesi 10 jam), FR-1.1 (backup manual: zip dev.db+uploads, diunduh via route terpisah). Diverifikasi end-to-end lewat HTTP (bukan cuma unit test): PIN salah, lockout, PIN benar → cookie sesi, akses ke `/api/documents/[id]` dan `/api/backup/[filename]` tanpa sesi ter-redirect ke /lock. Dua bug ditemukan+diperbaiki saat verifikasi: signature token sesi tidak menolak string yang ditempeli karakter di ujungnya (Buffer.from("hex") diam-diam membuang sisa ganjil), dan regex nama file backup menolak filename aslinya sendiri (kurang karakter "Z" dari ISO timestamp). Belum dikerjakan (Fase 2+): FR-1.2/1.3/1.4 (HDD/Sheets/Drive), FR-2B interim banner, FR-4 checklist, FR-11 auto-archive PDF. |
| v2-fase2 | 2026-08-04 | Phase 2 PRD — Fase 2: data flow & compliance UX | `instrumentation.ts` (baru), `lib/hddSync.ts` (baru), `lib/googleDrive/client.ts` (baru), `lib/actions/driveBackup.ts` (baru), `lib/actions/pdfArchive.tsx` (baru), `lib/actions/backupStatus.ts` (baru), `lib/status.ts` (breakdown FR-4 + trigger FR-1.3/FR-11), `lib/actions/document.ts`, `lib/storage.ts`, `components/detail/CompletionChecklist.tsx` (baru), `components/detail/DetailPrimitives.tsx`, `app/cdd/[id]/page.tsx`, `app/page.tsx`, `app/admin/backup/`, `.env.example`, `.gitignore` | FR-1.2 (sinkron HDD eksternal saat app start, via `instrumentation.ts`), FR-1.3 (export Google Sheets otomatis saat status jadi COMPLETE), FR-1.4 (backup scan ke Google Drive, fire-and-forget, tidak menunda OCR), FR-2B (banner interim Korporasi/LA — sudah ada dari v1, kata-kata diperjelas + anchor `#edd`), FR-4 (`getCompletionBreakdown()`/`computeCompletionBreakdown()` sebagai satu-satunya sumber breakdown, dipakai widget checklist di halaman detail dan tooltip "Menunggu: ..." di baris DRAFT dashboard), FR-11 (auto-archive PDF ke `storage/archive/` tepat saat transisi DRAFT→COMPLETE, titik pemicu tunggal di `computeAndPersistStatus`). Diverifikasi lewat skrip yang membuat customer uji, memicu transisi status, dan mengecek breakdown + file PDF hasil arsip secara langsung (bukan cuma type-check) — juga verifikasi UI sungguhan via HTTP: tooltip dashboard dan widget checklist di halaman detail. |
| v2-fase3 | 2026-08-04 | Phase 2 PRD — Fase 3: OCR trust & data quality | `lib/ocr/runOcr.ts` (word-level confidence+bbox), `lib/ocr/extractFields.ts` (`FieldGuess` = value+confidence+bbox, filter NPWP salah format), `lib/ocr/applyFieldGuesses.ts`, `lib/actions/document.ts`, `components/forms/OcrFieldContext.tsx` (baru), `components/forms/OcrReviewGate.tsx` (baru), `components/forms/OcrCroppedSnippet.tsx` (baru), `components/forms/fields.tsx` (three-state styling), `components/forms/IndividualForm.tsx`/`CorporateForm.tsx`/`LegalArrangementForm.tsx` (dipecah jadi outer+inner supaya context bisa dibaca), `lib/validations.ts` (`NIK_REGEX`/`isValidNpwpFormat`/`withNikCheck` — cross-field NIK-jika-KTP di 6 schema, NPWP format di 4 field), `lib/retention.ts` (baru), `app/cdd/[id]/page.tsx`, `app/admin/retensi/` (baru), `app/layout.tsx`, `SETUP.md` | FR-3 Must (three-state Empty/Suggested/Confirmed dengan tag "OCR" + border dashed, pre-submit review gate dismissable) dan Should (confidence Tesseract per-word di-capture & dipetakan balik ke tiap guess — dulu dibuang, guess dengan confidence <70 tampil merah bukan amber; cuplikan crop dari scan asli di field nama & no. identitas via `<canvas>`, hanya untuk field yang punya bbox). FR-12 (validasi ringan NIK 16 digit-jika-KTP & NPWP 15/16 digit, dicek di form manual lewat `superRefine` DAN di guess OCR sebelum ditampilkan sebagai suggestion). FR-5 baseline (`RETENTION_YEARS=5` — dikutip dari komentar INI, bukan primer, lihat komentar di `lib/retention.ts` — ditampilkan di halaman detail & laporan `/admin/retensi`; rekomendasi enkripsi FR-5 didokumentasikan di SETUP.md, tidak diimplementasikan). Diverifikasi: skrip langsung untuk `extractFieldGuesses` (confidence rata-rata, bbox gabungan, filter NPWP), validasi zod (NIK pendek+KTP ditolak, Paspor format bebas lolos), dan HTML SSR sungguhan via HTTP (badge "OCR" merah/amber sesuai confidence, halaman retensi mendeteksi CDD 6 tahun lalu sebagai overdue). Fase 4 (FR-7/8/9/13, backlog Could-have) BELUM dimulai — menunggu instruksi eksplisit sesuai arahan; FR-10 (goAML) sengaja tidak dikerjakan sama sekali. |
| v2-fase4a | 2026-08-04 | Phase 2 PRD — Fase 4 (backlog): FR-13 activity log | `prisma/schema.prisma` (model `ActivityLogEntry` baru + migrasi `add_activity_log`), `lib/activityLog.ts` (baru), `lib/actions/customer.ts`, `lib/actions/riskAssessment.ts`, `lib/actions/highRiskInfo.ts`, `components/detail/ActivityLogSection.tsx` (baru), `app/cdd/[id]/page.tsx` | FR-13 — log ringan append-only per customer (CDD dibuat, Risk Assessment disimpan/diperbarui, EDD disimpan/diperbarui), TANPA atribusi user (satu PIN bersama, sesuai FR-6B) — bukan sistem audit-trail resmi. `logActivity()` selalu try/catch, tidak pernah menggagalkan aksi utama yang memicunya. Ditampilkan sebagai timeline terbaru-dulu di halaman detail CDD. Diverifikasi: skrip langsung untuk `logActivity`/`getActivityLog` (urutan terbaru-dulu benar), dan HTML SSR sungguhan via HTTP menampilkan "Riwayat Aktivitas" dengan timestamp Indonesia. FR-7/8/9 (sisa backlog) BELUM dikerjakan — menunggu instruksi eksplisit lanjutan. |
| v2-fase4b | 2026-08-04 | Phase 2 PRD — Fase 4 (backlog): FR-9 deteksi klien lama, dievolusikan jadi cari-lalu-prefill | `prisma/schema.prisma` (index `noIdentitas`/`nomorHp`/`npwp`/`nomorTelepon` di 3 tabel detail + migrasi `add_dedup_lookup_indexes`), `lib/dedup.ts` (baru), `lib/actions/duplicateLookup.ts` (baru), `components/newcase/DuplicateLookupPanel.tsx` (baru), `app/cdd/new/page.tsx`, `app/cdd/new/{perorangan,korporasi,perikatan-lainnya}/page.tsx`, `components/forms/IndividualForm.tsx`/`CorporateForm.tsx`/`LegalArrangementForm.tsx`, `lib/actions/customer.ts` | FR-9, evolusi dari sekadar "warning duplikat" ke cari-lalu-prefill: di halaman "CDD Baru", notaris bisa cari klien lama lewat No. HP/Telepon atau No. Identitas/NPWP (dinormalisasi — "0812...", "+62812...", "62812..." dianggap sama; No. Identitas dibandingkan apa adanya minus spasi/strip supaya nomor paspor alfanumerik tetap kebaca). Keputusan desain (dikonfirmasi user): setiap hasil "Gunakan data ini" membuat **Customer baru** (bukan reuse row lama) — RiskAssessment/NotaryService/EDD tetap 1:1 per-engagement sesuai desain skema, cuma field identitas/kontak/pekerjaan yang di-prefill; `jasaYangDiberikan` sengaja dikosongkan karena melekat ke transaksi baru, bukan ke profil klien. Prefill gagal-diam ke null kalau id tak ada/tipe tak cocok (pola sama dengan `loadDraftDocument` OCR), dan digabung ke `defaultValues` SEBELUM `applyFieldGuesses` OCR supaya OCR baru tetap bisa mengisi sisa field kosong. Sumber prefill dicatat ke FR-13 activity log lewat `logActivity` saat submit. Diverifikasi: skrip langsung untuk `findPotentialDuplicates` (match No. HP lintas format, match NIK, no-false-positive) dan `loadIndividualPrefill` (nilai\+`jasaYangDiberikan` blank terverifikasi, guard tipe-salah balikin null), plus HTML SSR sungguhan via HTTP (banner prefill + data ter-passing benar ke form; halaman tipe-salah & tanpa param tetap bersih tanpa banner), plus verifikasi `logActivity` mencatat label sumber prefill. FR-7/8 (sisa backlog) BELUM dikerjakan. |
| v2-fase4c | 2026-08-04 | Phase 2 PRD — Fase 4 (backlog): FR-7 pengingat tinjau ulang periodik (PMPJ) | `prisma/schema.prisma` (field `Customer.lastReviewedAt` baru + migrasi `add_review_reminder`), `lib/reviewReminder.ts` (baru), `lib/actions/review.ts` (baru), `components/detail/MarkReviewedButton.tsx` (baru), `app/cdd/[id]/page.tsx`, `app/page.tsx` | FR-7 — PMPJ adalah kewajiban pemantauan berkelanjutan, bukan formulir sekali isi. Interval tinjau ulang: 1 tahun untuk kategori Tinggi (`REVIEW_INTERVAL_YEARS_HIGH_RISK`, disebutkan eksplisit di PRD sebagai contoh), 3 tahun untuk Sedang/Rendah/belum dinilai (`REVIEW_INTERVAL_YEARS_DEFAULT` — ASUMSI penulis karena PRD tidak menyebut angka untuk kategori ini, konfirmasi ke notaris/penasihat hukum). Tanggal jatuh tempo dihitung dari `lastReviewedAt ?? createdAt`. Halaman detail menampilkan tanggal jatuh tempo berikutnya (merah jika sudah lewat) + tombol "Tandai Sudah Ditinjau" yang mencatat `lastReviewedAt` dan menulis entry activity log. Dashboard punya filter tambahan "Jatuh tempo tinjau ulang" di form filter yang sudah ada, dihitung di memori dari data yang sudah di-fetch (bukan kolom tersimpan), pola sama dengan `/admin/retensi` untuk retensi. Diverifikasi: `npm run lint` + `npm run build` bersih, dan skrip langsung yang membuat customer uji Tinggi, mengecek interval 1 tahun + deteksi jatuh tempo, memanggil `markCustomerReviewed`, mengecek `lastReviewedAt` ter-update dan `isReviewOverdue` jadi false, mengecek activity log tercatat, lalu simulasi filter dashboard dengan 2 customer kombinasi risiko/tinjau berbeda — semua sesuai ekspektasi, customer uji dihapus setelahnya. Dikerjakan sebagai agent paralel terisolasi (worktree), diintegrasikan manual ke main. FR-8 (sisa backlog) BELUM dikerjakan. |

| v2-fase4d | 2026-08-04 | Phase 2 PRD — Fase 4 (backlog): FR-8 penanda LTKM + export | `prisma/schema.prisma` (field `Customer.isLtkm`/`ltkmNotes` baru + migrasi `add_ltkm_flag`), `lib/actions/ltkm.ts` (baru), `lib/ltkmCsv.ts` (baru), `components/detail/LtkmPanel.tsx` (baru), `app/cdd/[id]/page.tsx`, `app/page.tsx`, `app/admin/ltkm/` (baru), `app/api/ltkm-export/route.ts` (baru), `app/layout.tsx` | FR-8 — penanda manual "LTKM" (Laporan Transaksi Keuangan Mencurigakan) + catatan bebas per Customer, untuk membantu notaris menyiapkan submission goAML manual ke PPATK (bukan integrasi goAML — itu FR-10, sengaja di luar cakupan). Halaman detail punya banner merah + panel toggle/catatan (`LtkmPanel`, dua tombol terpisah — toggle status dan simpan catatan — supaya catatan bisa diedit tanpa tidak sengaja membalik status). Dashboard punya badge "LTKM" di baris yang ditandai + filter `?ltkm=1` di form filter yang sudah ada. `/admin/ltkm` (baru) melaporkan semua yang ditandai dengan tombol Export CSV; export sesungguhnya lewat route `app/api/ltkm-export/route.ts` (bukan Server Action, konsisten dengan konvensi file-serving lewat route), CSV dibangun manual sesuai RFC 4180 lewat `buildLtkmCsv()` (dipisah supaya bisa dites langsung, tanpa dependency baru). Bug nyata ditemukan+diperbaiki saat verifikasi: `nullifyEmpty` mengubah `""` jadi `undefined`, yang di `prisma.customer.update()` tanpa field lain berarti "biarkan nilai lama" bukan "kosongkan" — jadi menghapus tanda LTKM dengan catatan kosong diam-diam TIDAK menghapus catatan lama; diperbaiki dengan coalesce eksplisit ke `null`. Diverifikasi: `npm run lint` + `npm run build` bersih, skrip langsung untuk `setLtkmFlag` (tandai+catatan tersimpan, activity log tercatat, batalkan+catatan kosong benar-benar mengosongkan), skrip langsung untuk `buildLtkmCsv` (koma dan tanda kutip ganda di catatan ter-quote benar), dan HTTP sungguhan (`/admin/ltkm`, `/api/ltkm-export` dengan header Content-Type/Content-Disposition benar, banner+panel di halaman detail, badge+filter di dashboard). Dikerjakan sebagai agent paralel terisolasi (worktree), diintegrasikan manual ke main — migrasi asli agent di-generate ulang di main (bukan disalin mentah) karena skema dasarnya belum termasuk field FR-7 (`lastReviewedAt`) yang sudah lebih dulu terintegrasi, supaya kolom itu tidak ikut hilang saat SQLite table-rebuild. Backlog Could-have (FR-7/FR-8/FR-9/FR-13) kini lengkap semua; FR-10 (goAML) tetap sengaja tidak dikerjakan. |

| v2-fase6 | 2026-08-05 | Ops: app runner selalu aktif (PM2) + health endpoint | `ecosystem.config.js` (baru), `package.json` (`up`/`down`/`restart`/`status`/`logs` scripts, `pm2` devDependency), `app/api/health/route.ts` (baru), `proxy.ts` (kecualikan `api/health` dari PIN gate), `.gitignore` (`/logs/`), `SETUP.md` | Permintaan: satu perintah untuk menyalakan app di device lain (Windows, cmd atau bash) dan tetap hidup walau terminal ditutup / proses sempat crash — bukan auto-start setelah reboot Windows (scope eksplisit dikonfirmasi user, jadi TIDAK didaftarkan sebagai Windows Service). Dipilih PM2 dibanding container/Windows Service karena tidak butuh instalasi administrator dan sudah jadi standar de-facto untuk menjaga proses Node.js tetap hidup di satu mesin. `npm run up` = `next build` lalu `pm2 startOrReload ecosystem.config.js` (idempotent — aman dijalankan ulang kapan saja, termasuk untuk deploy ulang setelah `git pull`). `ecosystem.config.js` menjalankan `next start -H 127.0.0.1` (mempertahankan FR-6A, bukan `next dev`) dengan `autorestart`, `max_restarts`/`min_uptime` supaya crash-loop akibat bug asli tidak retry tanpa henti. `app/api/health/route.ts` (baru, dikecualikan dari PIN gate di `proxy.ts`) untuk cek cepat proses hidup tanpa login. Diverifikasi end-to-end sungguhan (bukan cuma baca dokumentasi PM2): `npm run up` di repo asli — proses ONLINE, `curl /api/health` balas 200 sementara `curl /` tetap redirect 307 ke `/lock` (membuktikan pengecualian PIN gate scoped dengan benar, bukan membuka semua route); PID proses di-`kill -9` langsung untuk simulasi crash — PM2 mendeteksi dan restart otomatis dalam ~2.4 detik (log `✓ Ready in 2.4s`, restart counter naik 0→1), health endpoint pulih setelah itu; `npm run down` mematikan bersih. `npm run lint` + `npx tsc --noEmit` bersih. Susulan sehari setelahnya: port distandarkan ke `4001` di SEMUA mode (`dev`/`start`/`up`) — sebelumnya `dev`/`start` tidak eksplisit set port (ikut default Next.js 3000) sementara `ecosystem.config.js` juga fallback ke 3000, jadi teknisnya sudah konsisten tapi implisit; permintaan user eksplisit menjadikannya fixed & terdokumentasi. `-p 4001` ditambahkan eksplisit ke keempat script (`dev`/`dev:lan`/`start`/`start:lan`) di `package.json`, `ecosystem.config.js` fallback diubah ke `"4001"`, `PORT="4001"` didokumentasikan di `.env.example`, dan section baru "Port & Host Standar" di `SETUP.md` menjelaskan kenapa host tetap `127.0.0.1` literal (bukan string `localhost`, menghindari ambiguitas resolusi `::1` IPv6) serta bagian mana saja yang harus diubah bersamaan kalau port pernah diganti (termasuk redirect URI OAuth "Lupa PIN" — sudah dynamic dari `request.url` di kode, jadi tidak perlu ubah kode, hanya dokumentasi + registrasi ulang di Google Cloud Console). Diverifikasi ulang: `npm run up` sungguhan mendengarkan di `127.0.0.1:4001` (health check 200, PIN gate masih redirect 307), port `3000` dikonfirmasi TIDAK dipakai sama sekali (`curl` gagal connect), `npm run lint` + `npx tsc --noEmit` bersih. |
| v2-fase7 | 2026-08-07 | `aml_phase_2_brief.md` Phase 3 — enkripsi PII at rest (subset FR-5) | `lib/documentEncryption.ts` (baru), `lib/ocr/runOcr.ts` (`extractTextFromImage` terima `Buffer`, bukan `filePath`), `lib/actions/document.ts`, `app/api/documents/[id]/route.ts`, `lib/actions/driveBackup.ts`, `.env.example`, `SETUP.md` | Sebelum fase ini, Phase 1/2/4/5 brief (network binding, PIN gate, backup manual, banner EDD) ternyata SUDAH terimplementasi di sesi-sesi sebelumnya (terverifikasi langsung ke kode, bukan cuma baca changelog) — brief ini baru mulai dari Phase 3. File scan KTP/dll. di `storage/uploads/` sekarang AES-256-GCM, kunci diturunkan dari `SESSION_SECRET` yang sudah ada (pola sama dengan `lib/ai/crypto.ts`, TIDAK ada env var wajib baru). OCR (Tesseract) dijalankan di memori dari buffer upload SEBELUM dienkripsi — plaintext gambar tidak pernah ditulis ke disk sama sekali, bahkan sementara. `driveBackup.ts` (FR-1.4) diperbaiki mendekripsi dulu sebelum upload ke Drive — kalau tidak, backup Drive akan diam-diam berisi byte terenkripsi alih-alih gambar valid (regresi nyata yang ditemukan+dicegah saat implementasi, bukan cuma teoretis). `hddSync.ts` (FR-1.2) dan `lib/actions/backup.ts` (FR-1.1) TIDAK perlu diubah — keduanya cuma menyalin byte mentah, jadi otomatis simetris dengan enkripsi. Residual risk didokumentasikan eksplisit di `SETUP.md` sesuai instruksi brief (jangan oversell): kunci tetap hidup di disk yang sama dengan data (`.env`), menaikkan standar terhadap "salin folder storage/" tapi TIDAK terhadap "akses penuh ke PC ini" — BitLocker tetap wajib, bukan tergantikan. `storage/uploads/` kosong saat fase ini dikerjakan, jadi tidak ada file lama yang perlu dimigrasi. Diverifikasi: `npx tsc --noEmit` + `npm run lint` + `npm run build` bersih, skrip langsung untuk round-trip enkripsi (cocok persis, ciphertext tidak mengandung magic bytes gambar asli, tamper terdeteksi lewat GCM auth tag), dan pipeline end-to-end sungguhan (OCR jalan dari buffer plaintext di memori → tulis ciphertext ke disk → baca ulang → dekripsi → byte identik dengan buffer asli) pakai PNG 1x1 nyata, bukan mock. Berhenti di sini sesuai instruksi brief "stop after each phase" — brief eksplisit meminta konfirmasi sebelum lanjut fase berikutnya. |
| v2-fase8 | 2026-08-07 | `aml_phase_2_brief.md` Phase 6 — cakupan test minimum + CI | `vitest.config.mts` (baru), `vitest.setup.ts` (baru), `lib/status.test.ts`/`lib/scoring.test.ts`/`lib/auth.test.ts`/`lib/documentEncryption.test.ts`/`lib/actions/backup.test.ts` (baru, 53 test), `lib/actions/backup.ts` (`createBackup` terima path override opsional untuk testability, default persis perilaku lama), `.github/workflows/ci.yml` (baru), `ecosystem.config.js` (eslint-disable bertarget untuk `no-require-imports` — file ini SENGAJA CommonJS karena dimuat PM2 lewat `require()`, bukan bug baru, cuma lint pre-existing yang harus dibersihkan supaya CI bisa hijau), `package.json` (`test` script + `vitest`/`unzipper`/`@types/unzipper` devDependencies), `SETUP.md` | Tidak ada test runner sebelumnya (dicek dulu sesuai instruksi brief) — pakai Vitest sesuai rekomendasi brief. Prioritas cakupan sesuai urutan brief: (1) `computeCompletionBreakdown()` di `lib/status.ts` — SEMUA cabang `cddDasar`/`powerOfAttorney`/`riskAssessment`/`eddOk` termasuk jalur hard-coded `eddOk=false` untuk Korporasi/LA berisiko Tinggi (Known Gap #2); (2) `computeRiskCategory()` di `lib/scoring.ts` — batas tiap kategori + gap di luar rentang; (3) kode Phase 1-5: `hashPin`/`verifyPinHash`/token sesi & lockout (`lib/auth.ts`), round-trip enkripsi dokumen (`lib/documentEncryption.ts`, termasuk deteksi tamper via GCM), integritas isi zip backup (`lib/actions/backup.ts`, dibuka lewat `unzipper` bukan cuma dicek ukuran file). Test zip backup dijalankan terhadap direktori sementara (`os.tmpdir()`) lewat parameter override baru di `createBackup()` — TIDAK PERNAH menyentuh `storage/` asli notaris. Bug/temuan nyata selama penulisan test: (a) asumsi awal bahwa `notaryService` kosong memblokir status COMPLETE ternyata SALAH — `overallComplete` di `lib/status.ts` hanya `cddComplete && riskComplete && eddOk`, `notaryService` cuma dihitung di rasio checklist UI, bukan gate status; didokumentasikan sebagai characterization test eksplisit ("current behavior, not necessarily intended"), TIDAK diubah tanpa konfirmasi eksplisit karena mengubah logika status di luar cakupan Phase 6; (b) `npm run lint` sudah gagal SEBELUM fase ini dimulai (pre-existing, dari `ecosystem.config.js` v2-fase6) — diperbaiki dengan eslint-disable bertarget karena acceptance criteria brief eksplisit minta "CI runs green on current main". Diverifikasi: simulasi urutan CI persis (hapus `lib/generated/prisma`, `npx prisma generate` tanpa `.env` sama sekali untuk membuktikan tidak butuh secret → lint → `tsc --noEmit` → `npm test`) — semua hijau, 53/53 test lolos; `npm run build` tetap bersih setelah semua perubahan. |
| v2-fase9 | 2026-08-07 | Security review pass (`/security-review`) + Strix skills install + dead-code/backend-frontend audit | `lib/backupArchive.ts` (baru), `lib/actions/backup.ts` (Server Action kembali tanpa parameter), `lib/actions/backup.test.ts` (import dari `lib/backupArchive.ts`, bukan Server Action), `components/admin/BackupPanel.tsx` (import tipe dari lokasi baru), `lib/transactionHelpers.ts` (baru — `ensureCaseForHighRisk` dipindah dari `lib/actions/case.ts`, `attachDraftDocument` dipindah dari `lib/actions/document.ts`), `lib/actions/riskAssessment.ts`/`lib/actions/customer.ts` (import dari lokasi baru), `lib/actions/aiSettings.ts` (3 Server Action mati dihapus: `getAiHealthSnapshotAction`/`getAiUsageSummaryAction`/`getRecentAiRequestsAction`), `lib/ai/provider.ts` (`PROVIDER_IDS` mati dihapus), `lib/actions/case.ts` (`getCaseByCustomerId` mati dihapus), `.github/workflows/security-review.yml` (baru), `.claude/skills/strix-*` (baru, 4 skill via `npx skills add usestrix/strix`), `security/strix-scan-instructions.md` (baru), `.gitignore` (`.strix-llm-key.local`, `/strix_runs/`) | Menjalankan `/security-review` lokal (butuh `git remote set-head origin main` dulu — repo belum pernah set `origin/HEAD`) menemukan bug NYATA yang baru ditulis sesi ini sendiri: `createBackup()`'s test-support `paths` override (v2-fase8) ternyata jadi Server Action publik reachable lewat POST langsung oleh siapa pun dengan sesi PIN valid, TERLEPAS dari argumen apa yang dikirim UI (`BackupPanel.tsx` cuma manggil tanpa argumen — tapi itu tidak mencegah panggilan langsung dengan argumen lain) — exploit: `{dbPath:".env"}` → zip berisi `.env` sebagai `dev.db`, nama file tetap lolos regex `/api/backup/[filename]` → unduh → `SESSION_SECRET` bocor → forge sesi + dekripsi semua dokumen ID klien (kunci Phase 3 diturunkan dari secret yang sama). Diperbaiki: logika archiving dipindah ke `lib/backupArchive.ts` (modul biasa, BUKAN `"use server"`, sehingga tidak pernah dapat action ID publik) — `createBackup()` kembali tanpa parameter, test sekarang impor `buildBackupZip()` langsung. Menyusul instruksi user ("cari dead code pakai graphify, cek integrasi backend-frontend, perbaiki yang rusak"): (1) audit dead-API — 10 route API semua reachable, `/api/health` sengaja tanpa caller in-app (health-check eksternal, dikecualikan PIN gate, terdokumentasi). (2) Dead-code sweep pakai `graphify query`/graph.json langsung (bukan cuma grep) — graph edge extraction-nya PUNYA blind spot nyata untuk JSX component usage & type-only reference (banyak false positive, mis. `BackupPanel()` muncul "0 incoming" padahal dipakai), jadi tiap kandidat diverifikasi ulang lewat text-search penuh (hitung kemunculan total termasuk file sendiri, bukan cuma exclude file sendiri — kesalahan pertama yang ditemukan & diperbaiki sendiri saat proses). Hasil: 5 export benar-benar mati dihapus (`getAiHealthSnapshotAction`/`getAiUsageSummaryAction`/`getRecentAiRequestsAction` di `aiSettings.ts` — duplikat dari `/api/ai/status/route.ts` + tidak ada UI usage/recent-requests sama sekali, `PROVIDER_IDS` di `provider.ts`, `getCaseByCustomerId` di `case.ts`). SENGAJA TIDAK dihapus: `lib/ai/services/{chat,ocr,risk,vision}.ts` — semua fungsinya punya komentar eksplisit "entry point untuk fitur masa depan" (arsitektur AI Processing Engine yang memang additive-only per desain, lihat memory), menghapusnya berarti menghapus infrastruktur yang sengaja dibangun duluan, bukan technical debt; `SkeletonText()` di `components/ui/skeleton.tsx` juga dibiarkan (bagian dari kit primitif Skeleton/SkeletonCard/SkeletonTable yang sudah dipakai, cuma varian ini belum, risiko rendah untuk disimpan). (3) Audit menyeluruh 22 file `"use server"` sungguhan (3 file lain false-positive — cuma menyebut string "use server" di komentar prosa) lewat 3 agent paralel, mencari pola kerentanan yang sama seperti bug backup.ts — NIHIL kerentanan baru ditemukan; SATU catatan konsistensi non-exploitable: `ensureCaseForHighRisk`/`attachDraftDocument` menerima `Prisma.TransactionClient` sebagai parameter pertama tapi di-export dari file `"use server"` (tidak actually exploitable — `tx` tidak bisa diserialisasi lewat wire RSC, panggilan langsung akan throw — tapi tidak konsisten dengan pola fix backup.ts), diperbaiki dengan memindahkan keduanya ke `lib/transactionHelpers.ts` (modul biasa) untuk defense-in-depth/konsistensi, bukan reaksi atas eksploit nyata. Ditambahkan juga: `.github/workflows/security-review.yml` (Action `anthropics/claude-code-security-review`, BUTUH `CLAUDE_API_KEY` sebagai GitHub secret yang harus ditambahkan manual oleh user — tidak bisa dilakukan dari sesi ini) dan 4 skill Strix (`strix-pentest`/`strix-ci-setup`/`strix-fix-findings`/`strix-cloud-api`) lewat `npx skills add usestrix/strix` — TIDAK dijalankan (scan live butuh Docker WSL integration + LLM API key milik user sendiri, user memilih menunda Strix demi fase ini dulu); `security/strix-scan-instructions.md` disiapkan lebih dulu, scoped eksplisit ke model auth PIN-tunggal aplikasi ini (PIN gate coverage, forgery token sesi, Server Action argument trust, path traversal, boundary enkripsi dokumen, CSRF OAuth) dan MENGECUALIKAN RBAC/privilege-escalation/multi-tenant karena aplikasi ini memang tidak punya konsep itu sama sekali. Diverifikasi: `npx tsc --noEmit` + `npm run lint` + `npm test` (53/53) + `npm run build` bersih setelah SETIAP perubahan (fix vulnerability, tiap penghapusan dead code, tiap pemindahan transaction helper). |
| v2-fase10 | 2026-08-07 | Security hardening pass (aditif, tanpa mengubah perilaku/arsitektur fundamental) | `next.config.ts` (header baru: `Permissions-Policy`/`Cross-Origin-Opener-Policy`/`X-DNS-Prefetch-Control`/`Content-Security-Policy` minimal), `lib/googleSheets/format.ts` (`fmtStr` sekarang menetralkan formula-injection), `lib/googleSheets/format.test.ts` (baru), `lib/fileSignature.ts` (baru — validasi magic bytes), `lib/fileSignature.test.ts` (baru), `lib/actions/document.ts`/`lib/actions/heroSettings.ts` (panggil `matchesFileSignature` sebelum simpan) | Diminta eksplisit: perkuat keamanan TANPA mengubah kode fundamental yang sudah ada — jadi setiap perubahan aditif murni, allowlist/behavior lama tidak berubah untuk file yang memang valid. Temuan utama (nyata, bukan teoretis): export Google Sheets (`sheetsExport.ts`/`sheetsFullSync.ts`, 51 titik panggil `fmtStr` gabungan) menulis via `valueInputOption=USER_ENTERED` (`lib/googleSheets/client.ts`) — SEMUA field bebas-teks CDD (nama, alamat, catatan, dll., diisi notaris ATAU saran OCR yang diterima) yang diawali `=`/`+`/`-`/`@` akan dieksekusi sebagai formula Sheets sungguhan saat dibuka (data exfiltration via `=HYPERLINK`/`=IMPORTXML`, dll.) — kelas kerentanan yang SAMA yang sudah pernah diperbaiki untuk export LTKM CSV (`lib/ltkmCsv.ts`, lihat riwayat commit) tapi TIDAK PERNAH diterapkan ke jalur Sheets. Diperbaiki di SATU titik (`fmtStr`, dipakai di 51 tempat) dengan teknik sama persis (prefix kutip tunggal) — tidak perlu sentuh `sheetsExport.ts`/`sheetsFullSync.ts` sama sekali. Header keamanan baru murni aditif (tidak menyentuh `script-src`/`style-src`/`img-src`, jadi nol risiko mematahkan Next.js/Tailwind) — diverifikasi dicek dulu ke kode bahwa app TIDAK PERNAH pakai `getUserMedia`/`mediaDevices` (upload foto scan pakai `<input type=file>` native, kamera diurus OS) sebelum mengunci `Permissions-Policy`. Validasi magic bytes (`lib/fileSignature.ts`) ditambahkan sebagai defense-in-depth kedua atas allowlist MIME yang sudah ada (`file.type` dari browser bisa dipalsukan) — `X-Content-Type-Options: nosniff` yang sudah ada sebelumnya SUDAH mencegah eksploitasi MIME-sniffing di jalur serve-balik, jadi ini bukan fix kerentanan aktif, murni pengerasan tambahan supaya konten yang tidak cocok tidak pernah tersimpan sama sekali. Dicek juga (bersih, tidak perlu perbaikan): `dangerouslySetInnerHTML` tidak dipakai di mana pun; `next` param di `/lock` sudah divalidasi (`startsWith("/") && !startsWith("//")`, cegah open redirect); `app/error.tsx`/`app/global-error.tsx` tidak pernah render `error.message`/`error.stack` ke user; cookie `secure:false` SUDAH benar untuk model deployment http://127.0.0.1-only yang terdokumentasi (mengubahnya jadi `true` justru akan MEMATAHKAN login, browser menolak kirim cookie secure lewat http://); proxy.ts matcher PIN gate sudah comprehensive; tidak ada `$queryRaw`/`$executeRaw`; tidak ada `.passthrough()` di skema Zod; console.log/error yang menyertakan `customerId` cuma logging cuid opaque, bukan PII. SENGAJA TIDAK dikerjakan: upgrade Next.js/PM2 untuk menutup `npm audit` findings (postcss/sharp/js-yaml) — itu breaking-version bump ke dependency inti, di luar cakupan "tanpa mengubah kode fundamental"; CSP dengan `script-src`/nonce — butuh wiring nonce lewat proxy.ts+layout.tsx yang jauh lebih invasif dan berisiko mematahkan hydration React, di luar cakupan yang diminta. Diverifikasi: `npx tsc --noEmit` + `npm run lint` + `npm test` (65/65, 12 test baru) + `npm run build` bersih; header baru DIVERIFIKASI SUNGGUHAN via `curl -I` ke server hasil `npm run start` sungguhan (bukan cuma baca kode) — semua header baru muncul persis seperti dikonfigurasi, halaman `/lock` tetap render normal (title + form PIN utuh), tidak ada breakage dari CSP. |
| v2-fase11 | 2026-08-07 | Fitur baru: Edit Data CDD (koreksi manual data client tersimpan) | `app/cdd/[id]/edit/page.tsx` (baru), `lib/actions/customerEdit.ts` (baru — `loadIndividualForEdit`/`loadCorporateForEdit`/`loadLegalArrangementForEdit`), `lib/actions/customerEdit.test.ts` (baru), `lib/customerFormMapping.ts` (baru — `toDateInputValue`/`formatShortDate`/`mapBeneficialOwner` dipindah dari `duplicateLookup.ts` karena file itu `"use server"` dan sinkron helper TIDAK BOLEH diexport dari situ), `lib/actions/duplicateLookup.ts` (import dari lokasi baru, hapus definisi lokal), `lib/actions/customer.ts` (`updateIndividualCustomer`/`updateCorporateCustomer`/`updateLegalArrangementCustomer` baru, paralel dengan `createXCustomer`), `components/forms/IndividualForm.tsx`/`CorporateForm.tsx`/`LegalArrangementForm.tsx` (prop `editCustomer` baru — form yang SAMA dipakai create & edit, bukan komponen terpisah), `app/cdd/[id]/page.tsx` (tombol "Edit Data" baru di header) | User (berperan sebagai Expert Solution Analyst) bertanya: kalau ada salah input manual data client yang sudah tersimpan, apakah bagus menyediakan UI edit, dan minta eksekusi langsung kalau ya. Rekomendasi yang diberikan sebelum eksekusi: BUKAN tabel edit gaya spreadsheet (~40-90 field terstruktur per tipe customer — enum, tanggal, array Beneficial Owner/Party bersarang, validasi silang NIK/NPWP — grid mentah akan melewati validasi & UX review-OCR yang sudah dibangun) — sebagai gantinya, form yang SAMA PERSIS yang sudah divalidasi Zod dipakai ulang dalam "mode edit", diisi-awal dari data tersimpan. Desain: `loadXForEdit` (baru, `lib/actions/customerEdit.ts`) MIRIP `loadXPrefill` (`duplicateLookup.ts`, dipakai fitur "cari klien lama" FR-9) tapi BEDA prinsip — prefill sengaja mengosongkan `jasaYangDiberikan` (melekat ke transaksi baru), loader edit sengaja MEMPERTAHANKAN semua field termasuk `jasaYangDiberikan` (mengedit record yang sama, bukan membuat baru). `updateXCustomer` meniru struktur `createXCustomer` persis (`$transaction`, `nullifyEmpty`, `toDate`) TAPI: (1) menolak kalau `customer.type` tersimpan tidak cocok dengan action yang dipanggil (defense-in-depth terhadap id yang salah/basi — ditest eksplisit), (2) Beneficial Owner/Party di-replace penuh (`deleteMany` lalu `createMany` dari array yang disubmit) bukan di-diff — field form tidak menyimpan id baris per-BO, dan tidak ada model lain yang mereferensikan `BeneficialOwner.id`/`LegalArrangementParty.id` (dicek ke `prisma/schema.prisma` dulu, aman). `computeAndPersistStatus` + `logActivity("...diperbarui (koreksi manual)")` dipanggil ulang sama seperti create — status DRAFT/COMPLETE bisa berubah kalau koreksi mengosongkan/mengisi field wajib, dan setiap koreksi tercatat di Riwayat Aktivitas (penting untuk software compliance AML/CDD — koreksi yang tidak tercatat adalah gap compliance tersendiri). Autosave-draft (`useAutosaveDraft`) dan banner recovery-draft dinonaktifkan total saat `editCustomer` aktif — key localStorage-nya sama dengan draft "CDD baru", tanpa ini isian edit-in-progress bisa menimpa/tertimpa draft customer baru yang tidak berhubungan. Dua bug ditemukan+diperbaiki saat implementasi: (a) `Pick<...>` dari tipe form (yang punya `.default([])` di Zod, jadi `beneficialOwners`/`parties` opsional di tipe *input*) bikin tsc protes "possibly undefined" walau nilainya selalu array nyata — diperbaiki dengan guard `&&` sama seperti pola `prefill` yang sudah ada; (b) helper `toDateInputValue`/`formatShortDate`/`mapBeneficialOwner` awalnya diexport langsung dari `duplicateLookup.ts` (`"use server"`) supaya dipakai bareng `customerEdit.ts` — Next.js build GAGAL ("Server Actions must be async functions") karena SEMUA export dari file `"use server"` wajib jadi Server Action async; dipindah ke `lib/customerFormMapping.ts` (modul biasa), pola yang sama dengan `lib/backupArchive.ts` (v2-fase9). Diverifikasi jauh lebih dalam dari sekadar tsc/lint/test: script langsung yang memanggil `updateIndividualCustomer` gagal dengan "Invariant: static generation store missing" — `redirect()`/`revalidatePath()` adalah primitif runtime Next.js yang TIDAK BISA jalan di luar request sungguhan, sama seperti `createXCustomer` yang sudah ada (`createIndividualCustomer` dkk juga tidak bisa dites lewat script biasa, alasan kenapa create action tidak pernah punya unit test) — solusinya `vi.mock("next/navigation")`/`vi.mock("next/cache")` di `customerEdit.test.ts` (baru, 8 test) yang menjalankan logika transaksi SUNGGUHAN terhadap Prisma client asli (`prisma/dev.db`, tidak ada DB test terpisah di proyek ini), tiap test bikin+hapus sendiri customer uji di `afterEach` (fixture pertama sempat lolos dari cleanup karena error di percobaan skrip manual pertama sebelum pindah ke pendekatan Vitest — ditemukan lewat query eksplisit "customer tersisa di DB?" dan dihapus manual, dikonfirmasi `prisma.customer.count()` kembali ke 0). Juga diverifikasi sungguhan lewat HTTP: `npm run start` asli + token sesi PIN asli (`createSessionToken()`) + `curl` — halaman `/cdd/[id]/edit` balas 200 dengan RSC flight payload berisi data DB asli (termasuk `jasaYangDiberikan` tidak dikosongkan), tombol "Simpan Perubahan" (bukan "Simpan CDD Perorangan"), banner peringatan tampil; TANPA cookie sesi redirect 307 ke `/lock` (PIN gate otomatis mencakup route baru ini, tidak perlu ubah `proxy.ts`); link "Edit Data" di halaman detail mengarah ke URL yang benar. Customer uji HTTP juga dihapus setelah verifikasi, `prisma.customer.count()` dikonfirmasi 0 lagi. |
| v2-fase12 | 2026-08-08 | Auto-detect NPWP/NIK inline di form CDD + chart dashboard baru + data dummy + greeting waktu presisi + auto-lock idle 5 menit | `lib/hooks/useDuplicateFieldMatch.ts` (baru), `components/forms/DuplicateFieldBanner.tsx` (baru), `components/forms/IndividualForm.tsx`/`CorporateForm.tsx`/`LegalArrangementForm.tsx` (wiring auto-detect + `appliedSourceLabel` untuk activity log), `lib/dashboard.ts` (`getRiskByCustomerType` baru), `components/dashboard/RiskByTypeChart.tsx` (baru), `app/page.tsx` (chart baru diwire + greeting pakai `lib/greeting.ts`), `lib/greeting.ts` (baru, dipisah dari `app/page.tsx`), `lib/greeting.test.ts` (baru), `scripts/seed-dummy-dashboard-data.ts` + `scripts/remove-dummy-dashboard-data.ts` (baru), `components/layout/IdleLockTimer.tsx` (baru), `app/layout.tsx` (wiring), `components/layout/LockButton.tsx` (komentar basi diperbaiki) | User minta dicek via graphify apakah form CDD sudah auto-fill field lain saat NPWP/NIK diketik langsung (BUKAN dari kotak pencarian terpisah `DuplicateLookupPanel` yang sudah ada) — dikonfirmasi belum ada, langsung diimplementasi sesuai instruksi ("jika belum langsung aplikasikan"). Reuse total: TIDAK ada Server Action baru — `useDuplicateFieldMatch` cuma wiring client baru di atas `findPotentialDuplicates()`/`loadXPrefill()` yang sudah ada (FR-9), debounce 600ms + minimal 6 digit sebelum cari, difilter ke tipe customer form yang sedang dibuka. TIDAK PERNAH auto-isi diam-diam — selalu lewat banner konfirmasi eksplisit ("Isi Otomatis"/"Abaikan"), pola human-in-the-loop yang sama dengan review OCR (FR-3) dan `DuplicateLookupPanel` yang sudah ada. Dimatikan total di mode edit (`editCustomer`) — mencari kecocokan pakai NIK/NPWP milik record yang sedang diedit sendiri tidak pernah relevan. `sourceLabel` hasil auto-detect disatukan dengan `sourceLabel` dari alur URL prefill supaya activity log tetap konsisten mencatat asal data. Bug lint nyata: `setCandidate(null)` sinkron di badan `useEffect` kena `react-hooks/set-state-in-effect` — diperbaiki dengan memindahkan SEMUA `setCandidate` (termasuk jalur "clear") ke dalam `setTimeout` (delay 0ms untuk clear, `DEBOUNCE_MS` untuk pencarian), bukan cuma jalur pencarian. Chart dashboard baru: dicek dulu `RiskChart.tsx` (SUDAH stacked bar — waktu x kategori risiko) supaya tidak duplikat, chart baru pakai dimensi berbeda (jenis customer x kategori risiko, `RiskByTypeChart`) — palet warna disamakan persis dengan RiskChart/RiskDonut. Data dummy (`scripts/seed-dummy-dashboard-data.ts`, 15 customer — 7 Perorangan/5 Korporasi/3 Perikatan Lainnya, tersebar RENDAH/SEDANG/TINGGI, tanggal dibuat disebar mundur ~55 hari) SEMUA nama diawali `[DUMMY]` supaya tidak pernah tertukar data klien asli, idempotent (skip kalau data dummy sudah ada), termasuk skrip kebalikannya untuk hapus. Efek samping organik yang justru jadi verifikasi tambahan: customer TINGGI (baik Perorangan tanpa EDD, maupun Korporasi/Legal Arrangement — Known Gap #2) BENAR tidak ikut auto-archive PDF, membuktikan logika status/EDD existing tetap berjalan benar dengan data variatif. Bug operasional ditemukan+diperbaiki saat verifikasi HTTP: server `next start` yang sedang dipakai untuk curl ternyata proses ZOMBIE dari sesi verifikasi SEBELUMNYA (PID beda, `pkill` pattern-based sempat tidak mengenainya) — chart baru & greeting baru tidak muncul di HTML karena curl memukul build LAMA, bukan build yang baru saja dibuat; ditemukan lewat cross-check `ps aux` STIME vs mtime source file, diperbaiki dengan `kill -9` PID eksplisit + rebuild + restart bersih sebelum lanjut verifikasi (pelajaran: `pkill -f` tidak selalu bisa diandalkan membersihkan proses `next start` lama di sesi panjang, cross-check PID+timestamp kalau hasil curl mencurigakan). Greeting hero card: rentang lama (Pagi<11, Siang<15, Sore<19) diganti presisi menit sesuai spek user (Pagi 00:00-09:00, Siang 09:01-15:00, Sore 15:01-18:00, Malam 18:01-23:59) — fungsi awalnya ditulis di `app/page.tsx` tapi test-nya bikin waktu impor test naik dari ~8s ke ~29s (menarik seluruh graph dependency dashboard yang berat) — dipindah ke `lib/greeting.ts` (modul kecil berdiri sendiri), waktu tes kembali normal. Auto-lock idle 5 menit (`IdleLockTimer`): komentar LAMA di `LockButton.tsx` eksplisit bilang "tanpa idle-timeout otomatis" — dikonfirmasi memang belum ada, sekarang diimplementasi sebagai pelengkap (bukan pengganti) `LockButton` (kunci manual) dan `SessionExpiryWarning` (masa berlaku sesi 10 jam). Reuse `logout()` (Server Action yang sama dipakai `LockButton`) dipanggil langsung sebagai fungsi (bukan lewat `<form>`) — pola yang sama persis dengan `createXCustomer` dipanggil dari `onSubmit` form CDD (sudah terbukti `redirect()` di dalamnya tetap jalan normal walau dipanggil begitu, bukan lewat form action), jadi auto-lock SUNGGUHAN menghapus cookie sesi di server (bukan cuma redirect tampilan client) — kembali lewat tombol back browser tetap diminta PIN lagi. Listener aktivitas (mousemove/mousedown/keydown/scroll/touchstart) di-attach ke `window` dengan `{passive:true}`, dimatikan di halaman `/lock` sendiri. Diverifikasi: `npx tsc --noEmit` + `npm run lint` + `npm test` (77/77, 4 test baru) + `npm run build` bersih; data dummy dijalankan sungguhan (15 customer + PDF auto-archive terverifikasi selektif sesuai EDD rules); chart data diverifikasi langsung lewat `getRiskByCustomerType()` DAN lewat HTML/RSC flight payload sungguhan via `curl` (setelah bug server zombie di atas diperbaiki) — angka cocok persis; pencarian auto-detect diverifikasi langsung lewat `findPotentialDuplicates()` dengan NIK/NPWP dummy yang sama persis yang didokumentasikan ke user untuk uji manual; greeting diverifikasi HTML sungguhan cocok dengan jam server saat itu (06:32 WIB -> "Selamat Pagi"); `IdleLockTimer` dikonfirmasi ter-render di RSC flight payload halaman dashboard. Timer 5-menit sendiri TIDAK bisa diuji end-to-end di sesi ini (butuh browser sungguhan + menunggu 5 menit nyata, tidak tersedia) — kepercayaan berbasis code review + kesamaan pola `logout()`/`redirect()` yang sudah terbukti, bukan observasi langsung; disebutkan eksplisit ke user sebagai batas verifikasi. |

| v2-fase13 | 2026-08-08 | Hardening komersial V1 — P0: upgrade PIN hashing, enkripsi field OCR, verifikasi backup; P1: log keamanan, enforcement retensi | `lib/auth.ts` (scrypt PIN hashing, `isLegacyPinHash`, migrasi SHA-256 otomatis), `lib/actions/auth.ts` (rehash transparan + wiring security log), `lib/actions/pinRecovery.ts` (wiring security log PIN_RESET), `lib/actions/backup.ts` (wiring security log BACKUP_CREATED), `lib/documentEncryption.ts` (`encryptString`/`decryptString`/`encryptJson`/`decryptJsonField` — format `$enc$v1$`), `lib/actions/document.ts` (enkripsi `ocrRawText`/`fieldGuesses` on write, dekripsi on read), `lib/backupArchive.ts` (`manifest.json` di dalam ZIP + SHA-256 ZIP di `meta.json`, `verifyBackupChecksum()`), `scripts/hash-pin.ts` (pakai `hashPin()` dari lib/auth), `.env.example` (update docs PIN_HASH), `lib/storage.ts` (`SECURITY_LOG_PATH`), `lib/securityLog.ts` (baru — NDJSON append-only security event log, 8 tipe event, `logSecurityEvent`/`getSecurityEvents`), `lib/actions/retention.ts` (baru — `deleteCustomerRecord`: hapus file disk + cascade DB + security event), `components/admin/RetentionDeleteButton.tsx` (baru — client component dengan ConfirmDialog), `app/admin/retensi/page.tsx` (tambah tombol Hapus permanen), `app/admin/security-log/page.tsx` (baru — halaman log keamanan admin), `components/layout/Sidebar.tsx` (tambah link Log Keamanan), `.gitignore` (`/storage/security.log`), `lib/auth.test.ts`/`lib/documentEncryption.test.ts`/`lib/actions/backup.test.ts` (test baru untuk semua perubahan P0). P0: (1) PIN SHA-256 → scrypt N=2^15/r=8/p=1 + salt 16 byte, format `$scrypt$...`, legacy SHA-256 auto-detected & auto-rehash saat login berhasil. (2) OCR data di DB kini AES-256-GCM sama seperti file scan — format `$enc$v1$...`, data lama plaintext masih dibaca dengan benar (auto-detect). (3) Backup kini menyertakan `manifest.json` berisi SHA-256 tiap file + SHA-256 seluruh ZIP di `meta.json`. P1: (1) Security event log file-based (`storage/security.log`, NDJSON) merekam login/logout/lockout/PIN reset/backup/hapus data — ditampilkan di halaman Admin > Log Keamanan. (2) Retensi data enforcement: tombol "Hapus Permanen" di halaman Admin > Retensi Data dengan ConfirmDialog 2-step — hapus file dari disk + cascade DB + catat ke security log. P0-1 (Tabel B "config required") dan P0-5 (AI governance) sudah terimplementasi sebelumnya — tidak ada perubahan tambahan. P1-Corporate EDD dan P1-Individual accounts sengaja TIDAK dikerjakan: EDD Korporasi tidak ada formulir resmi (Known Gap #2), individual accounts di luar cakupan V1 (by design single-user). |

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
